[package]
name = "ethnum-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[features]
llvm-intrinsics = ["ethnum/llvm-intrinsics"]

[dependencies]
afl = "0.12"
arbitrary = { version = "1", features = ["derive"] }
ethnum = { path = ".." }
num = "0.4"
